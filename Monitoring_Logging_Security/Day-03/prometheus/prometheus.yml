global:
  scrape_interval: 5s

scrape_configs:
  # Application metrics
  - job_name: "application"
    static_configs:
      - targets: ["app:3000"]

  # System metrics (Node Exporter)
  - job_name: "node_exporter"
    static_configs:
      - targets: ["node-exporter:9100"]